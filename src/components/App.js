import React, { Component } from 'react'
import Metrics from './Metrics'

// We can decide what page should be shown for user from here
export default class App extends Component {
    render() {
        return (
            <Metrics />
        );
    }
}
